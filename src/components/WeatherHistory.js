// src/pages/WeatherHistory.js
import React from 'react';
import WeatherHistory from '../components/WeatherHistory';

function WeatherHistoryPage() {
  // Fetch data or receive props as needed
  return (
    <div>
      <WeatherHistory />
    </div>
  );
}

export default WeatherHistoryPage;
